# HideChannelListShortcuts
Hides shortcut entries at the top of the channel list, such as `Events` and `Server Boosts`.

## Notes
- Works by watching the channel list DOM and hiding matching rows
- Can hide `Events`, `Server Boosts`, or both
- Attempts to clean up orphaned separators left behind by hidden rows
