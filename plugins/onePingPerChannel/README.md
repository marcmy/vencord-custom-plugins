# OnePingPerChannel
Only plays the first notification per unread channel. Reading the channel resets the limit, with an optional cooldown timer.

## Purpose
- Prevents notification spam in busy channels
- Keeps a single audio ping per unread channel (or a timed cooldown)
- Resets once you read the channel
